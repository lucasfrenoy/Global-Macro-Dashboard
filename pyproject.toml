[project]
name = "global-macro-dashboard"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "“lucasfrenoy”", email = "“lucas.frenoy@skema.edu”" }
]
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.6",
    "streamlit>=1.64.0",
    "yfinance>=1.7.0",
]

[project.scripts]
global-macro-dashboard = "global_macro_dashboard:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
